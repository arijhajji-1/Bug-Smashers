{% extends 'base-front.html.twig' %}

{% block title %} Montage{% endblock %}

{% block body %}



    <hr style="visibility: hidden ">
    <div  class="container" style="max-width: 600px">

        {% form_theme form 'bootstrap_4_layout.html.twig' %}

        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
        <h1 align="center">Choisir les composants</h1>

        {{ form_end(form) }}

        <div class="col-12 mt-22">
            <button class="btn btn-brand"><a href="{{ path ('addmontage') }} "style="color: white;">Ajouter</a> </button>
            <button class="btn btn-brand"> <a href="{{ path('affiche') }}"style="color: white;" >votre demande</a></button>
        </div>
        <br>

        <br>
    </div>


{% endblock %}
