{% extends 'base-back.html.twig' %}

{% block title %}Publication{% endblock %}

{% block body %}
    <div class="container">
    <h1>Publication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publication.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ publication.titre }}</td>
            </tr>
            <tr>
                <th>Desription</th>
                <td>{{ publication.desription }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ publication.image }}</td>
            </tr>
        </tbody>
    </table>

        {{ include('publication/_delete_form.html.twig') }}
    <a href="{{ path('publication_index') }}" class="btn btn-info mt-2" >back to list</a>

    </div>
{% endblock %}
