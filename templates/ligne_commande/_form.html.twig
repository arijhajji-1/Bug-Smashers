{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('SAUVEGARDER LES MODIFICATIONS') }}</button>
{{ form_end(form) }}
